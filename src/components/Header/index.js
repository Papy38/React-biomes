import "./Header.css";

export default function Header() {
    return (
        <header>
            <span>Biomes</span>
            <nav>
                <ul>

                </ul>
            </nav>
            <select name="biomes">
                <option value="jungle" />
                <option value="idk" />
            </select>
        </header>
    );
}